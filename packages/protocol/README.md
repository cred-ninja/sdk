# @credninja/protocol

Shared Cred wire-protocol constants for Cred clients and servers.

```typescript
import {
  CRED_PROTOCOL_VERSION,
  CRED_PROTOCOL_VERSION_HEADER,
  isCredProtocolVersionSupported,
} from '@credninja/protocol';
```
